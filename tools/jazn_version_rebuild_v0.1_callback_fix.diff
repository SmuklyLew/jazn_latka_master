--- a/tools/jazn_version_rebuild.py
+++ b/tools/jazn_version_rebuild.py
@@ -561,9 +561,9 @@
             try:
                 result = worker()
             except Exception as exc:
-                self.after(0, lambda: self.failed(exc))
+                self.after(0, self.failed, exc)
             else:
-                self.after(0, lambda: self.succeeded(result, done))
+                self.after(0, self.succeeded, result, done)
 
         threading.Thread(target=run, daemon=True).start()
 
